services:
  playwright:
    build: .
    image: vibeprod-playwright-mcp:latest
    container_name: vibeprod-playwright
    restart: unless-stopped
    init: true
    networks:
      - vibeprod-mcp

networks:
  vibeprod-mcp:
    name: vibeprod-mcp
